method mono(n: Int, x : Int) returns (o: Int[MONODOWN[n]]) {
    var i : Int

    i := 0
    o := 0
    while (i < n) {

        if (x < n) {
            o := o + 2
        } else {
            o := o + 1
        }
        i := i + 1
    }
}