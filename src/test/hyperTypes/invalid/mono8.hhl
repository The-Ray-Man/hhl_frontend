method mono(n: Int) returns (o: Int[MONODOWN[n]]) {
    
    var i : Int
    i := 0

    while (i < n) {
        i := i + 1
    }

    i := i * (-1)

    while (i > n) {
        i := i - 1
    }

    o := i
}