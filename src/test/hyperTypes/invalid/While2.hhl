method testWhileHighAffectsLow(s1: Int[low], s2: Int[high]) returns (o : Int[low])
{
    var s2new : Int
    s2new := s2
    o := s1
    while (s2new > 0) {
        o := o + 1
        s2new := s2new - 1
    }
}
