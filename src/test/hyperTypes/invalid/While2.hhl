method testWhileHighAffectsLow(s1: Int[LOW], s2: Int) returns (o : Int[LOW])
{
    var s2new : Int
    s2new := s2
    o := s1
    while (s2new > 0) {
        o := o + 1
        s2new := s2new - 1
    }
}
