

method fib(n : Int, k : Int[LOW]) returns (o: Int[MONOUP[n]])
{
    var a : Int
    var b : Int
    var i : Int
    var tmp : Int
    a := 0
    b := 1
    i := 0
    while (i < n)
    {
        tmp := b
        b := a + b
        a := tmp
        i := i + 1
    }
    o := b
}