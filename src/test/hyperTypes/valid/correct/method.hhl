method bar(x: Int[LOW]) returns (o: Int) {
    o := x
}


method foo(x: Int[LOW], y : Int) returns (o : Int) {
    
    var tmp : Int
    o := 1
    tmp := x

    if (y < 0) {
        o := bar(tmp)
    } else {
        tmp := tmp + 1
    }
}