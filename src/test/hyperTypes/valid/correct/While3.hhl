method flipWhile(s1: Int[LOW], x : Int[LOW, POS], s2: Int) returns (o : Int[LOW])
{
    var s1new : Int
    var xnew : Int
    xnew := x
    s1new := s1
    o := 0
    while (s1new > 0) {
        xnew := (-1) * xnew
        o := 1
    }
}
