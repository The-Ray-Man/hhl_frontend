method mono(n: Int, x : Int[LOW], y: Int[LOW]) returns (o: Int[MONOUP[n]]) {
    var i : Int

    i := 0
    o := 0
    while (i < n) {
        if (x < y) {
            o := o + 2
        } else {
            o := o + 1
        }
        i := i + 1
    }
}