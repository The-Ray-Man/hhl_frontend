method testSafeBranching(s1: Int[low], s2: Int[low]) returns (o : Int[low])
{
    o := 0
    while (s1 > 0) {
        if (s1 % 2 == 0) {
            o := o + 2
        } else {
            o := o + 1
        }
        s1 := s1 - 1
    }
}
